name: End of Day Summary (16:15 ET, DST-safe)

on:
  schedule:
    - cron: "15 20 * * 1-5"   # 16:15 ET during EDT (UTC-4)
    - cron: "15 21 * * 1-5"   # 16:15 ET during EST (UTC-5)
  workflow_dispatch:

jobs:
  gate:
    runs-on: ubuntu-latest
    outputs:
      proceed: ${{ steps.check.outputs.proceed }}
    steps:
      - id: check
        shell: bash
        run: |
          if [ "${{ github.event_name }}" != "schedule" ]; then
            echo "Manual/dispatch run — proceeding regardless of time."
            echo "proceed=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          TARGET_MIN=975        # 16:15 America/New_York (minutes since midnight)
          TOLERANCE=20          # minutes of GitHub scheduling delay to tolerate
          H=$(TZ=America/New_York date +%-H)
          M=$(TZ=America/New_York date +%-M)
          NOW_MIN=$(( H * 60 + M ))
          DIFF=$(( NOW_MIN - TARGET_MIN ))
          printf 'Eastern now %02d:%02d | diff %d min | tolerance %d\n' "$H" "$M" "$DIFF" "$TOLERANCE"
          if [ "$DIFF" -ge 0 ] && [ "$DIFF" -le "$TOLERANCE" ]; then
            echo "In window — proceeding."
            echo "proceed=true" >> "$GITHUB_OUTPUT"
          else
            echo "Out of window (wrong-season cron or delayed beyond tolerance) — skipping."
            echo "proceed=false" >> "$GITHUB_OUTPUT"
          fi

  eod_summary:
    needs: gate
    if: needs.gate.outputs.proceed == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Download NLTK data
        run: |
          python -c "
          import nltk
          for pkg in ['punkt', 'punkt_tab', 'averaged_perceptron_tagger', 'averaged_perceptron_tagger_eng']:
              try: nltk.download(pkg, quiet=True)
              except: pass
          "

      - name: Create data directory
        run: mkdir -p data logs

      - name: Run EOD summary session
        env:
          ALPACA_API_KEY:    ${{ secrets.ALPACA_API_KEY }}
          ALPACA_SECRET_KEY: ${{ secrets.ALPACA_SECRET_KEY }}
          ALPACA_BASE_URL:   https://paper-api.alpaca.markets
          NEWS_API_KEY:      ${{ secrets.NEWS_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          DRY_RUN:           ${{ vars.DRY_RUN || 'false' }}
        run: python main.py --session eod_summary

      - name: Commit trade log and reports
        run: |
          git config user.name "trading-bot"
          git config user.email "bot@trading.local"
          git pull --rebase origin main || true
          git add data/trades.db data/live_feed.json || true
          git add reports/*.html || true
          git diff --staged --quiet || git commit -m "bot: eod_summary trade log + daily report [skip ci]"
          git push || true
